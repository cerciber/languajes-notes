-- Separar las instrucciones individuales (Ejecuta las instrucciones en un solo lote)
(instruccion_1);
(instruccion_2);
(instruccion_N);

-- Separar las instrucciones en lotes (El siguiente Lote no se ejecuta hasta que finalice el anterior)
NO SOPORTADO POR MYSQL

-- Cambiar separador de instrucciones
DELIMITER nuevo_separador