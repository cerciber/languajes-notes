
// Cualquier clase de excepción
exception = Error;
	// Error al evaluar una expresión 
	exception = EvalError;
	// Indice de algun tipo fuera de rango 
	exception = RangeError;
	// No se puede acceder a un objeto por su nombre 
	exception = ReferenceError;
	// Error de sintaxis 
	exception = SyntaxError;
	// Error cuando la entrada es un tipo de dato diferente al esperado 
	exception = TypeError;
