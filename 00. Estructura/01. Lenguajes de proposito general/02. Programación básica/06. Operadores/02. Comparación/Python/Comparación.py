 
# Mayor que
variable = expresión1 > expresión2

# Mayor igual que
variable = expresión1 >= expresión2

# Menor que
variable = expresión1 < expresión2

# Menor igual que
variable = expresión1 <= expresión2

# Igual que
variable = expresión1 == expresión2		# Igual valor
variable = expresión1 is expresión2		# Para objetos

# Diferente a  
variable = expresión1 != expresión2		# Diferente valor
variable = expresión1 is not expresión2	# Para objetos

# Contenido en
variable = expresión1 in lista_o_iterable

# No contenido en
variable = expresión1 not in lista_o_iterable