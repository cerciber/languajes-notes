   
// Estandar
do {
    // Contenido
	continue;		// Saltar a la verificación de condición
    // Contenido	
	break;			// Salir del ciclo
    // Contenido
} while (condición);

// Con retorno
arreglo = []; 
do {
	arreglo.push(expresión);
} while (condición);
