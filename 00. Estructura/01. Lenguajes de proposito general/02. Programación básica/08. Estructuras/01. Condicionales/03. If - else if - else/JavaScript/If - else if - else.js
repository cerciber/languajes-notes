
// Estandar 
if (condición1) {
    // Contenido 1
} else if (condición2) {
    // Contenido 2
} else if (condiciónN) {
    // Contenido N
} else {
	// Contenido default
}

// Con retorno
retorno = condición1 ? expresión1 : condición2 ? expresión2 : expresiónFalsa;
