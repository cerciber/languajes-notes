package prueba;

public class Prueba {

    public static void main(String[] args) {
        
        // Matematicas
        Math.abs(valor);            // Valor absoluto
        Math.atan(valor);           // Arcotangente
        Math.sin(valor);            // Seno (En radianes)
        Math.cos(valor);            // Coseno (En radianes)
        Math.tan(valor);            // Tangente (En radianes)
        Math.exp(valor);            // Exponencial
        Math.log(valor);            // Logaritmo natual
        Math.log10(valor);          // Logaritmo base 10
        Math.pow(valor1, valor2);   // Potencia
        Math.round(valor);          // Redondear a entero
        Math.ramdom();              // Numero aleatorio entre 0 y 1
        Math.ceil(valor);           // Techo
        Math.floor(valor);          // Piso

    }
    
}
