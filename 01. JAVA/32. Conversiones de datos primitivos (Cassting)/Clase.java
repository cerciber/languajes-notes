package prueba;

public class Clase {

    public static void main(String[] args) {

        boolean vBoolean = false;
        byte vByte = 0;
        char vChar = 'a';
        short vShort = 0;
        int vInt = 0;
        long vLong = 0l;
        float vFloat = 0.0f;
        double vDouble = 0.0d;
        String vString = "";
        
        // Boleano      a       booleano
        vByte = vByte;
        
        // Boleano      a       byte
        vByte = (vBoolean ? (byte) 1 : (byte) 0);
        
        // Boleano      a       char
        vChar = (vBoolean ? (char) 1 : (char) 0);
        
        // Booleano     a       short
        vShort = (vBoolean ? (short) 1 : (short) 0);
        
        // Booleano     a       int
        vInt = (vBoolean ? (int) 1 : (int) 0);
        
        // Booleano     a       long
        vLong = (vBoolean ? (long) 1 : (long) 0);
        
        // Booleano     a       float
        vFloat = (vBoolean ? (float) 1 : (float) 0);
        
        // Booleano     a       double
        vDouble = (vBoolean ? (double) 1 : (double) 0);
        
        // Booleano     a       string
        vString = String.valueOf(vBoolean);
        
        // byte         a       boolean
        vBoolean = (vByte == 1);
        
        // byte         a       byte
        vByte = vByte;
        
        // byte         a       char
        vChar = (char) vByte;
        
        // byte         a       short
        vShort = (short) vByte;
        
        // byte         a       int
        vInt = (int) vByte;
        
        // byte         a       long
        vLong = (long) vByte;
        
        // byte         a       float
        vFloat = (float) vByte;
        
        // byte         a       double
        vDouble = (double) vByte;
        
        // byte         a       string
        vString = String.valueOf(vByte);
        
        // char         a       boolean
        vBoolean = (vChar == 1);
        
        // char         a       byte
        vByte = (byte) vChar;
        
        // char         a       char
        vChar = (char) vChar;
        
        // char         a       short
        vShort = (short) vChar;
        
        // char         a       int
        vInt = (int) vChar;
        
        // char         a       long
        vLong = (long) vChar;
        
        // char         a       float
        vFloat = (float) vChar;
        
        // char         a       double
        vDouble = (double) vChar;
        
        // char         a       string
        vString = String.valueOf(vChar);
        
        // short        a       boolean
        vBoolean = (vShort == 1);
        
        // short        a       byte
        vByte = (byte) vShort;
        
        // short        a       char
        vChar = (char) vShort;
        
        // short        a       short
        vShort = (short) vShort;
        
        // short        a       int
        vInt = (int) vShort;
        
        // short        a       long
        vLong = (long) vShort;
        
        // short        a       float
        vFloat = (float) vShort;
        
        // short        a       double
        vDouble = (double) vShort;
        
        // short        a       string
        vString = String.valueOf(vShort);
        
        // int          a       boolean
        vBoolean = (vInt == 1);
        
        // int          a       byte
        vByte = (byte) vInt;
        
        // int          a       char
        vChar = (char) vInt;
        
        // int          a       short
        vShort = (short) vInt;
        
        // int          a       int
        vInt = (int) vInt;
        
        // int          a       long
        vLong = (long) vInt;
        
        // int          a       float
        vFloat = (float) vInt;
        
        // int          a       double
        vDouble = (double) vInt;
        
        // int          a       string
        vString = String.valueOf(vInt);
        
        // long         a       boolean
        vBoolean = (vLong == 1);
        
        // long         a       byte
        vByte = (byte) vLong;
        
        // long         a       char
        vChar = (char) vLong;
        
        // long         a       short
        vShort = (short) vLong;
        
        // long         a       int
        vInt = (int) vLong;
        
        // long         a       long
        vLong = (long) vLong;
        
        // long         a       float
        vFloat = (float) vLong;
        
        // long         a       double
        vDouble = (double) vLong;
        
        // long         a       string
        vString = String.valueOf(vLong);
        
        // float        a       boolean
        vBoolean = (vFloat == 1);
        
        // float        a       byte
        vByte = (byte) vFloat;
        
        // float        a       char
        vChar = (char) vFloat;
        
        // float        a       short
        vShort = (short) vFloat;
        
        // float        a       int
        vInt = (int) vFloat;
        
        // float        a       long
        vLong = (long) vFloat;
        
        // float        a       float
        vFloat = (float) vFloat;
        
        // float        a       double
        vDouble = (double) vFloat;
        
        // float        a       string
        vString = String.valueOf(vFloat);
        
        // double       a       boolean
        vBoolean = (vDouble == 1);
        
        // double       a       byte
        vByte = (byte) vDouble;
        
        // double       a       char
        vChar = (char) vDouble;
        
        // double       a       short
        vShort = (short) vDouble;
        
        // double       a       int
        vInt = (int) vDouble;
        
        // double       a       long
        vLong = (long) vDouble;
        
        // double       a       float
        vFloat = (float) vDouble;
        
        // double       a       double
        vDouble = (double) vDouble;
        
        // double       a       string
        vString = String.valueOf(vDouble);
        
        // String       a       boolean
        vBoolean = (vString == "1");
        
        // String       a       byte
        vByte = Byte.valueOf(vString);
        
        // String       a       char
        vChar = vString.charAt(0);
        
        // String       a       short
        vShort = Short.valueOf(vString);
        
        // String       a       int
        vInt = Integer.valueOf(vString);
        
        // String       a       long
        vLong = Long.valueOf(vString);
        
        // String       a       float
        vFloat = Float.valueOf(vString);
        
        // String       a       double
        vDouble = Double.valueOf(vString);
        
        // String       a       string
        vString = String.valueOf(vString);
        
    }

}
