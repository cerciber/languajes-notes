<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.cerciber.prueba.MainActivity">

    <!-- Button = Elemento para detectar el click de mouse -->

        <!-- android:id = identificador del elemento -->

        <!-- android:layout_width = distribucion del ancho -->
        <!-- android:layout_height = distribucion del alto -->

            <!-- "wrap_content" = atrapar contenido -->
            <!-- "match_parent" = ajustar al elemento padre -->

        <!-- android:text = texto del elemento -->

        <!-- style = estilo -->

            <!-- ?android:borderlessButtonStyle = sin borde -->

        <!-- android:drawableLeft = dibujar una imagen a la izquierda -->
        <!-- android:drawablePadding = margen entre el boton y la imagen -->

        <!-- android:drawablePadding = margen entre el boton y la imagen -->

        <Button 
            android:id="@+id/boton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Texto boton" 
            style="?android:borderlessButtonStyle"/>

</RelativeLayout>
    
