# importar libreria de soporte para vectores y matrices
import numpy as np

# Funcion escalar
def funcion(a, b, c):
	# Contenido

# Vectorizar función (Convertir a función universal)
funcion2 = np.vectorize(funcion)

# Llamar función vectorizada (Acepta numeros, vectores y matrices por parametro) (Aplica la transmisión)
funcion2(matriz1, matriz2, matriz3)