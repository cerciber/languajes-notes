
#include <string.h>

int main() {

    //Cadena de caracteres (Finaliza con el caracter \0)
    char cadena[] = "cadena de texto";

    strlen(cadena);							// Longitud de la cadena
    strcmp(cadena, cadena2);				// Comparar cadenas
    strcpy(cadenaDestino, cadenaOrinen);	// Copiar cadena					

}
