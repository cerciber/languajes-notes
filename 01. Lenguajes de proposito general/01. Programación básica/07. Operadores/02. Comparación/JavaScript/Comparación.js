 
// Mayor que
variable = expresión1 > expresión2;

// Mayor igual que
variable = expresión1 >= expresión2;

// Menor que
variable = expresión1 < expresión2;

// Menor igual que
variable = expresión1 <= expresión2;

// Igual que
variable = expresión1 == expresión2;	// Igual valor
variable = expresión1 === expresión2;	// Igual valor e igual tipo

// Diferente a  
variable = expresión1 != expresión2;	// Diferente valor
variable = expresión1 !== expresión2;	// Diferente valor o diferente tipo

