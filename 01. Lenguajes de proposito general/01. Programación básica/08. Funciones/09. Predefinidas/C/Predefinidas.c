
#include <math.h>   
#include <time.h>

int main() {
    
    // Matemáticas
    floor(valor)            // Piso
    round(valor)            // Redondear
    ceil(valor)             // Techo
    sin(valor)              // Seno
    cos(valor)              // Coseno
    cosh(valor)             // Coseno hiperbólico
    exp(valor)              // Exponencial
    tan(valor)              // Tangente
    tanh(valor)             // Tangente hiperbólico
    sinh(valor)             // Seno hiperbólico
    log(valor)              // Logaritmo natural
    log10(valor)            // Logaritmo base 10
    sqrt(valor)             // Raiz cuadrada
    pow(valor, potencia)    // Potencia
    trunc(valor)            // Truncar

    // Numeros aleatorios
    rand()                  // Obtener numero aleatorio
    srand(time(NULL))       // Obtener numero aleatorio en base a la hora actual

    // Fechas
    time_t fecha        // Crear variable tipo fecha
    time(NULL)          // Obtener la hora actual
    ctime()             // Convertir a string

}

