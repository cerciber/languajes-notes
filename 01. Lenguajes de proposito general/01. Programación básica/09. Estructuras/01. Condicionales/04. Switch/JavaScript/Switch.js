
// Estandar
switch (expresión) {
    case valor1:
        // Contenido 1
        break;
    case valor2:
        // Contenido 2
        break;
    case valorN:
        // Contenido N
        break;
    default:
        // Contenido default
}

// Con retorno
retorno = expresión === valor1 ? expresión1 : expresión === valor2 ? expresión2 : expresión === valorN ? expresiónN : expresiónDefault;
