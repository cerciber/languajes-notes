NO SOPORTADO POR PYTHON
EN SU LUGAR SE USAN VALORES POR DEFECTO

class Clase:
    
    # Definición de metodo con atributos por defecto
    def metodo(atributo1 = valor_por_defecto1, atributo2 = valor_por_defecto2, atributoN = valor_por_defectoN):
        # Contenido
    
# Instanciar clase
clase1 = Clase()

# Llamar metodos sobrecargados
clase1.metodo()
clase1.metodo(atributo1)
clase1.metodo(atributo1, atributo2)
clase1.metodo(atributo1, atributo2, atributoN)
